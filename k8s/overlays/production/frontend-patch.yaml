apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: policy-rag
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: frontend
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1Gi
